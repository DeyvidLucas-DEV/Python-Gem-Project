# Arquivo de exemplo para ambiente de PRODUÇÃO na VPS
# Copie este arquivo para .env na sua VPS e ajuste os valores

# Database
DATABASE_URL=postgresql+asyncpg://usuario:senha@localhost:5432/gem_db

# Security (IMPORTANTE: Gere uma nova SECRET_KEY em produção!)
# Para gerar: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=ALTERE_ESTA_CHAVE_SECRETA_EM_PRODUCAO
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# API Settings
PROJECT_NAME=Sistema de Publicações Acadêmicas
VERSION=1.0.0
API_V1_STR=/api/v1

# CORS - Configure as origens do seu frontend
# Exemplo com domínio e localhost para testes:
BACKEND_CORS_ORIGINS=https://seu-frontend.com,http://localhost:3000,http://localhost:8080

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
